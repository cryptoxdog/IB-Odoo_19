# Odoo PlasticOS payload schemas

Draft payload contracts produced by IB-Odoo_19 (SoR). Peers:

- supply-opportunity / buyer-demand (TASK-039)
- canonical-projection / sync-projection / outcome-feedback (TASK-042)

Shared `$defs` live in `draft/common.schema.yaml`. Native validators for the
projection/outcome family are in `scripts/projection_outcome_schemas.py`.
