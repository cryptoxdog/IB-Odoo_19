# Baseline ledger: deprecated PacketEnvelope references (fail-closed debt).
# Enforced by l9-ci-sdk baseline ratchet — see .github/workflows/baseline-ratchet.yml.
# IB-Odoo_19 consumes TransportPacket exclusively through constellation-node-sdk
# inside plasticos_gate (single-egress invariant, enforced by
# tests/test_gate_single_egress.py) and has ZERO deprecated PacketEnvelope
# findings at adoption, so this ledger is intentionally empty. Any future
# PacketEnvelope reference fails CI.
# Changes to this file require CODEOWNERS review (.github/CODEOWNERS).
schema_version: 1.0.0
gate: pre-commit/packet-envelope-prohibited
entries: []
