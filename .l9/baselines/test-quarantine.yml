# Baseline ledger: quarantined pre-existing test failures (fail-closed debt).
# Enforced by l9-ci-sdk baseline ratchet — see .github/workflows/baseline-ratchet.yml.
# IB-Odoo_19 has ZERO failing pure-Python tests at adoption (296 passed /
# 10 skipped, verified on the Staging baseline), so this ledger is
# intentionally empty. Any future test failure fails CI unless a reviewed
# ledger entry with owner, issue, and expiry is added in the same PR.
# The 10 skips are conditional environment skips (Odoo runtime absent),
# reported by pytest as skips — not failures — and need no ledger entries.
# Changes to this file require CODEOWNERS review (.github/CODEOWNERS).
schema_version: 1.0.0
gate: tests/pytest
entries: []
