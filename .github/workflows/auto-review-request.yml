name: Auto Request Review (disabled)

on:
  workflow_dispatch:  # deactivated — was pinging cryptoxdog on every external PR

jobs:
  request-review:
    name: Request Review from Owner
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto review request is disabled."
