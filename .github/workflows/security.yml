# REMOVED — all checks duplicated by ci.yml
# - secret-scan: covered by ci.yml secret-scan job (SHA-pinned gitleaks)
# - trivy: advisory-only (exit-code: 0), no unique blocking value
# - pip-audit: advisory-only (continue-on-error: true), no unique blocking value
#
# To re-enable, uncomment and update ci.yml instead.
#
# This file is kept as a tombstone to prevent re-creation.
# Last active SHA: 60101042a7f9ad2027c39d8e623cfd736602ed51

name: Security Scan (disabled)

on:
  workflow_dispatch:  # manual only — all jobs moved to ci.yml

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security scanning is handled by ci.yml secret-scan job."
