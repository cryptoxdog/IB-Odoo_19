<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Odoo 19: utm.source no longer has an `active` field (only utm.medium /
         utm.campaign do). Prior deactivation records writing active=False are
         invalid and blocked install-smoke. Keep irrelevant stock sources out of
         pickers via UI/domain filters or by not referencing them — do not write
         active on utm.source. -->
</odoo>
