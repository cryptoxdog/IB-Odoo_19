# PlasticOS DEVELOPER / CI-only Python dependencies
#
# IMPORTANT: This file is NOT read by Odoo.sh. Odoo.sh only installs packages
# from requirements.txt. Anything here runs ONLY in local dev or GitHub Actions
# tooling — never inside the production Odoo runtime.
#
# Install locally with:  pip install -r requirements-dev.txt

# Pure-Python test runner (pins match .github/workflows/ci.yml pure-python-tests job).
# pytest-timeout enables [tool.pytest.ini_options] timeout settings in pyproject.toml.
# pytest-cov provides the advisory coverage report (config: [tool.coverage.*]); non-gating.
pytest==8.3.5
pytest-timeout==2.4.0
pytest-cov==5.0.0

# Type checker (advisory in CI — pin matches .github/workflows/ci.yml static-checks job,
# see [tool.mypy] in pyproject.toml). Unpinned before 2026-07; local mypy could silently
# disagree with CI's pinned version. NOT the same tool as Pyright/Pylance (editor-only,
# see [tool.pyright] in pyproject.toml) — mypy is the CI/pre-commit-facing checker.
mypy==1.14.0

# Checker pins — match .github/workflows/ci.yml (lint + static-checks).
ruff==0.16.0
semgrep==1.164.0

# PR Repair engine — used by scripts/pr_repair_adapter.py for the
# state-machine-governed CI repair loop (Issue→Finding conversion, approval
# gates, repeated-failure guard). Pinned to a specific commit SHA for
# reproducible builds.
# To upgrade: fetch the new SHA and update the @<sha> below.
#   gh api repos/cryptoxdog/PR_Repair/commits/main --jq '.sha'
pr-repair @ git+https://github.com/cryptoxdog/PR_Repair.git@bb364e427c7f5ebe4efc1a50aa5fe04e94105b38
