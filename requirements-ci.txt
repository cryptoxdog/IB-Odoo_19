# Minimal dependency manifest for the GATE-01 baseline ratchet
# (.github/workflows/baseline-ratchet.yml). The ratchet re-runs the pure-Python
# test tier ("Tier 3" of ci.yml) in a clean environment; tests/conftest.py
# auto-deactivates every Odoo-importing test module, so no Odoo runtime or
# constellation-node-sdk is needed here. The pure web-lead attachment component
# directly imports the already-declared requests runtime dependency.
#
# Pins match .github/workflows/ci.yml (pure-python-tests job) and
# requirements-dev.txt — keep the shared pure-test dependencies in lockstep.
pytest==8.3.5
pytest-timeout==2.4.0
pytest-cov==5.0.0
requests>=2.28.0
