<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- ── List View ───────────────────────────────────────── -->
  <record id="plasticos_material_form_view_list" model="ir.ui.view">
    <field name="name">plasticos.material.form.list</field>
    <field name="model">plasticos.material.form</field>
    <field name="arch" type="xml">
      <list editable="bottom">
        <field name="sequence" widget="handle"/>
        <field name="name"/>
        <field name="code"/>
        <field name="description"/>
      </list>
    </field>
  </record>

  <!-- ── Action ──────────────────────────────────────────── -->
  <record id="plasticos_material_form_action" model="ir.actions.act_window">
    <field name="name">Material Forms</field>
    <field name="res_model">plasticos.material.form</field>
    <field name="view_mode">list</field>
  </record>
</odoo>
