<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <!-- ═══════════════════════════════════════════════════════════════════
             SUPPLIER / BROKER FOLLOW-UP TEMPLATES
             ═══════════════════════════════════════════════════════════════════ -->

        <!-- Supplier Follow-up Template -->
        <record id="mail_template_supplier_followup" model="mail.template">
            <field name="name">PlasticOS: Supplier Readiness Follow-up</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="subject">Follow-up: Readiness Confirmation Required — {{ object.name }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.partner_id.name }},</p>
<p>This is an automated follow-up regarding Purchase Order <strong>{{ object.name }}</strong>.</p>
<p>We have not yet received confirmation that the load is ready for pickup.
Please confirm readiness at your earliest convenience.</p>
<p>Follow-up attempt: #{{ object.x_followup_count }}</p>
<p>Thank you,<br/>PlasticOS Logistics</p>
            ]]></field>
            <field name="email_from">{{ (object.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.partner_id.email }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

        <!-- Broker Follow-up Template -->
        <record id="mail_template_broker_followup" model="mail.template">
            <field name="name">PlasticOS: Broker Receipt Follow-up</field>
            <field name="model_id" ref="stock.model_stock_picking"/>
            <field name="subject">Follow-up: Receipt Confirmation Required — {{ object.name }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.x_broker_id.name }},</p>
<p>This is an automated follow-up regarding Delivery Order <strong>{{ object.name }}</strong>.</p>
<p>We have not yet received confirmation of receipt of the dispatch packet.
Please confirm receipt at your earliest convenience.</p>
<p>Follow-up attempt: #{{ object.x_broker_followup_count }}</p>
<p>Thank you,<br/>PlasticOS Logistics</p>
            ]]></field>
            <field name="email_from">{{ (object.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.x_broker_id.email }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

        <!-- ═══════════════════════════════════════════════════════════════════
             LOGISTICS LOAD DOCUMENT TEMPLATES (Paperless Send)
             ═══════════════════════════════════════════════════════════════════ -->

        <!-- Delivery Order Email Template -->
        <record id="mail_template_delivery_order" model="mail.template">
            <field name="name">PlasticOS: Delivery Order to Carrier</field>
            <field name="model_id" ref="plasticos_logistics.model_plasticos_load"/>
            <field name="subject">DELIVERY ORDER {{ object.name }} — {{ object.pickup_partner_id.city or '' }} to {{ object.delivery_partner_id.city or '' }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.carrier_contact_name or object.carrier_id.name }},</p>

<p>Please find attached the <strong>Delivery Order #{{ object.name }}</strong> for your records.</p>

<table style="border-collapse: collapse; width: 100%; margin: 15px 0;">
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Rate:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">${{ "%.2f"|format(object.rate_amount) }} USD</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup Location:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_partner_id.name }}<br/>{{ object.pickup_partner_id.street or '' }}<br/>{{ object.pickup_partner_id.city or '' }}, {{ object.pickup_partner_id.state_id.code or '' }} {{ object.pickup_partner_id.zip or '' }}</td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup Date:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_datetime }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Delivery Location:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_partner_id.name }}<br/>{{ object.delivery_partner_id.street or '' }}<br/>{{ object.delivery_partner_id.city or '' }}, {{ object.delivery_partner_id.state_id.code or '' }} {{ object.delivery_partner_id.zip or '' }}</td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Delivery Date:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_datetime }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Product:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.product_description or 'See attached' }}</td>
    </tr>
</table>

<p><strong>Important:</strong></p>
<ul>
    <li>Obtain LIGHT &amp; HEAVY 3rd Party Certified Scale Tickets</li>
    <li>Use the attached BOL-PICKUP document at origin</li>
    <li>Send all completed docs to {{ object.sale_order_id.company_id.email or 'docs@company.com' }}</li>
</ul>

<p>Please confirm receipt of this Delivery Order by replying to this email.</p>

<p>Thank you,<br/>
{{ object.sale_order_id.company_id.name or 'PlasticOS Logistics' }}<br/>
{{ object.sale_order_id.company_id.phone or '' }}</p>
            ]]></field>
            <field name="email_from">{{ (object.sale_order_id.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.carrier_id.email }}</field>
            <field name="report_template_ids" eval="[(4, ref('plasticos_logistics.action_report_delivery_order'))]"/>
            <field name="auto_delete" eval="False"/>
        </record>

        <!-- BOL Pickup Email Template -->
        <record id="mail_template_bol_pickup" model="mail.template">
            <field name="name">PlasticOS: BOL Pickup to Carrier</field>
            <field name="model_id" ref="plasticos_logistics.model_plasticos_load"/>
            <field name="subject">BOL - PICKUP {{ object.name }} — {{ object.pickup_partner_id.name or '' }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.carrier_contact_name or object.carrier_id.name }},</p>

<p>Please find attached the <strong>Bill of Lading - Pickup #{{ object.name }}</strong>.</p>

<p><strong>IMPORTANT - DOUBLE BLIND PICKUP:</strong><br/>
Use this BOL document at the pickup location. Do NOT share delivery destination details with the shipper.</p>

<table style="border-collapse: collapse; width: 100%; margin: 15px 0;">
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup At:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_partner_id.name }}<br/>{{ object.pickup_partner_id.street or '' }}<br/>{{ object.pickup_partner_id.city or '' }}, {{ object.pickup_partner_id.state_id.code or '' }} {{ object.pickup_partner_id.zip or '' }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup Contact:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_contact_name or '' }}<br/>{{ object.pickup_contact_phone or '' }}</td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup Date:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_datetime }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Reference #:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_reference or '' }}</td>
    </tr>
</table>

<p>Please have the shipper sign the BOL and return a copy to {{ object.sale_order_id.company_id.email or 'docs@company.com' }}.</p>

<p>Thank you,<br/>
{{ object.sale_order_id.company_id.name or 'PlasticOS Logistics' }}</p>
            ]]></field>
            <field name="email_from">{{ (object.sale_order_id.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.carrier_id.email }}</field>
            <field name="report_template_ids" eval="[(4, ref('plasticos_logistics.action_report_bol_pickup'))]"/>
            <field name="auto_delete" eval="False"/>
        </record>

        <!-- BOL Delivery Email Template -->
        <record id="mail_template_bol_delivery" model="mail.template">
            <field name="name">PlasticOS: BOL Delivery to Carrier</field>
            <field name="model_id" ref="plasticos_logistics.model_plasticos_load"/>
            <field name="subject">BOL - DELIVERY {{ object.name }} — {{ object.delivery_partner_id.name or '' }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.carrier_contact_name or object.carrier_id.name }},</p>

<p>Please find attached the <strong>Bill of Lading - Delivery #{{ object.name }}</strong>.</p>

<p>Use this BOL document at the delivery location for the consignee signature.</p>

<table style="border-collapse: collapse; width: 100%; margin: 15px 0;">
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Deliver To:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_partner_id.name }}<br/>{{ object.delivery_partner_id.street or '' }}<br/>{{ object.delivery_partner_id.city or '' }}, {{ object.delivery_partner_id.state_id.code or '' }} {{ object.delivery_partner_id.zip or '' }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Delivery Contact:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_contact_name or '' }}<br/>{{ object.delivery_contact_phone or '' }}</td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Delivery Date:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_datetime }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Reference #:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_reference or '' }}</td>
    </tr>
</table>

<p>Please have the consignee sign the BOL and return a copy to {{ object.sale_order_id.company_id.email or 'docs@company.com' }}.</p>

<p>Thank you,<br/>
{{ object.sale_order_id.company_id.name or 'PlasticOS Logistics' }}</p>
            ]]></field>
            <field name="email_from">{{ (object.sale_order_id.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.carrier_id.email }}</field>
            <field name="report_template_ids" eval="[(4, ref('plasticos_logistics.action_report_bol_delivery'))]"/>
            <field name="auto_delete" eval="False"/>
        </record>

        <!-- Combined Dispatch Packet (All 3 docs) -->
        <record id="mail_template_dispatch_packet" model="mail.template">
            <field name="name">PlasticOS: Full Dispatch Packet to Carrier</field>
            <field name="model_id" ref="plasticos_logistics.model_plasticos_load"/>
            <field name="subject">DISPATCH PACKET {{ object.name }} — {{ object.pickup_partner_id.city or '' }} to {{ object.delivery_partner_id.city or '' }}</field>
            <field name="body_html"><![CDATA[
<p>Dear {{ object.carrier_contact_name or object.carrier_id.name }},</p>

<p>Please find attached the complete <strong>Dispatch Packet for Load #{{ object.name }}</strong>.</p>

<p><strong>Attached Documents:</strong></p>
<ol>
    <li><strong>Delivery Order</strong> — Your dispatch instructions and rate confirmation</li>
    <li><strong>BOL - Pickup</strong> — Use at origin (PROTECT THE BLIND)</li>
    <li><strong>BOL - Delivery</strong> — Use at destination for consignee signature</li>
</ol>

<table style="border-collapse: collapse; width: 100%; margin: 15px 0;">
    <tr style="background-color: #2c3e50; color: white;">
        <td colspan="2" style="padding: 10px;"><strong>LOAD SUMMARY</strong></td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd; width: 30%;"><strong>Rate:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">${{ "%.2f"|format(object.rate_amount) }} USD</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Pickup:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.pickup_partner_id.name }}, {{ object.pickup_partner_id.city or '' }} {{ object.pickup_partner_id.state_id.code or '' }}<br/>{{ object.pickup_datetime }}</td>
    </tr>
    <tr style="background-color: #f5f5f5;">
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Delivery:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.delivery_partner_id.name }}, {{ object.delivery_partner_id.city or '' }} {{ object.delivery_partner_id.state_id.code or '' }}<br/>{{ object.delivery_datetime }}</td>
    </tr>
    <tr>
        <td style="padding: 10px; border: 1px solid #ddd;"><strong>Product:</strong></td>
        <td style="padding: 10px; border: 1px solid #ddd;">{{ object.product_description or 'See attached' }}</td>
    </tr>
</table>

<p><strong>REQUIREMENTS:</strong></p>
<ul>
    <li>✓ Obtain LIGHT &amp; HEAVY 3rd Party Certified Scale Tickets</li>
    <li>✓ Use BOL-PICKUP at origin — PROTECT THE BLIND</li>
    <li>✓ Use BOL-DELIVERY at destination for consignee signature</li>
    <li>✓ Send all completed docs to {{ object.sale_order_id.company_id.email or 'docs@company.com' }}</li>
</ul>

<p><strong>Please confirm receipt of this dispatch packet by replying to this email.</strong></p>

<p>Thank you,<br/>
{{ object.sale_order_id.company_id.name or 'PlasticOS Logistics' }}<br/>
{{ object.sale_order_id.company_id.phone or '' }}</p>
            ]]></field>
            <field name="email_from">{{ (object.sale_order_id.company_id.email or user.email) }}</field>
            <field name="email_to">{{ object.carrier_id.email }}</field>
            <field name="report_template_ids" eval="[(4, ref('plasticos_logistics.action_report_delivery_order')), (4, ref('plasticos_logistics.action_report_bol_pickup')), (4, ref('plasticos_logistics.action_report_bol_delivery'))]"/>
            <field name="auto_delete" eval="False"/>
        </record>

    </data>
</odoo>
